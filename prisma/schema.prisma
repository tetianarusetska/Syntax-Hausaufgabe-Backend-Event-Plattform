generator client {
  provider = "prisma-client"
  output   = "./client"
}

datasource db {
  provider = "postgresql"
}

model Event {
  id       Int      @id @default(autoincrement())
  title    String
  location String
  date     DateTime
  isPublic Boolean  @default(false)
}